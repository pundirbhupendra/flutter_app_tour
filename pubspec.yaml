name: flutter_app_tour
description: A lightweight, customizable guided tour overlay for Flutter apps.
version: 0.0.2
repository: https://github.com/example/flutter_app_tour

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
